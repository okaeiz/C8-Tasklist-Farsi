{
  "name": "C8-Tasklist-Farsi",
  "version": "1.0.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "docker-compose up",
    "stop": "docker-compose down",
    "build": "docker-compose build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
